const domainAdmin = "/admin"
export const ROUTERS = {
     USER: {
          HOME: "",
          PROFILE: "profile",
     },
     ADMIN: {
          HOME: domainAdmin + "",
          MENU: domainAdmin + "/Menus"
     }
}